apiVersion : v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app: my-app
spec:
  containers:
    - name: my-container
      image: nginx:latest
#      volumeMounts:
#        - name: my-volume
#          mountPath: /usr/share/nginx/html
    - name: my-sidecar
      image: ubuntu:latest
      command: ["sh", "-c", "tail -f /dev/null"]
#      volumeMounts:
#        - name: my-volume
#          mountPath: /cachePOP
#  volumes:
#    - name: my-volume
#      emptyDir: {}