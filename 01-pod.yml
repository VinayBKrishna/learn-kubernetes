apiVersion : v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app: my-app
spec:
  containers:
    - name : my-container
      image: nginx:latest
    - name: my-sidecar
      image: busybox:latest
      command: ["sh", "-c", " echo 'Hello from sidecar'"]